#include <stdio.h>

int main(int argc, char **argv)
{
    // argc tells us how many strings there are
    // argv is an array of strings, i.e. an array of an array of characters
    printf("Hello, World!\n");
    
}
